{
  "_doc": "Reusable bar chart panel: shows total battery drop percentage grouped by mode.",
  "_usage": "Copy this panel into any dashboard. Reads pre-computed battery_rates from InfluxDB (computed by extract-bag.py Pass 1b using same algorithm as mission_time_analysis). Replace bucket and mission filter.",
  "datasource": {
    "type": "influxdb",
    "uid": "${DS_INFLUXDB}"
  },
  "fieldConfig": {
    "defaults": {
      "color": {
        "mode": "continuous-GrYlRd"
      },
      "custom": {
        "axisCenteredZero": false,
        "axisColorMode": "text",
        "axisLabel": "Battery Drop (%)",
        "axisPlacement": "left",
        "fillOpacity": 80,
        "gradientMode": "none",
        "hideFrom": {
          "legend": false,
          "tooltip": false,
          "viz": false
        },
        "lineWidth": 1,
        "scaleDistribution": {
          "type": "linear"
        },
        "thresholdsStyle": {
          "mode": "off"
        }
      },
      "mappings": [],
      "thresholds": {
        "mode": "absolute",
        "steps": [
          {"color": "green", "value": null},
          {"color": "red", "value": 80}
        ]
      },
      "unit": "short"
    },
    "overrides": []
  },
  "gridPos": {
    "h": 8,
    "w": 12,
    "x": 0,
    "y": 0
  },
  "options": {
    "barRadius": 0,
    "barWidth": 0.89,
    "colorByField": "Battery Drop (%)",
    "fullHighlight": false,
    "groupWidth": 0.7,
    "legend": {
      "calcs": [],
      "displayMode": "list",
      "placement": "right",
      "showLegend": true
    },
    "orientation": "auto",
    "showValue": "always",
    "stacking": "none",
    "tooltip": {
      "mode": "single",
      "sort": "none"
    },
    "xField": "mode",
    "xTickLabelRotation": 0,
    "xTickLabelSpacing": 0
  },
  "targets": [
    {
      "_comment": "Reads pre-computed total drop from battery_rates measurement (computed by extract-bag.py matching mission_time_analysis algorithm).",
      "query": "from(bucket: \"vessel-data\")\n  |> range(start: 0)\n  |> filter(fn: (r) => r._measurement == \"battery_rates\")\n  |> filter(fn: (r) => r._field == \"total_drop_pct\")\n  |> filter(fn: (r) => r.mission == \"${mission}\")\n  |> filter(fn: (r) => r.mode != \"UNKNOWN\" and r.mode != \"NO_BAG_RECORD\" and r.mode != \"NO_DATA\")\n  |> keep(columns: [\"mode\", \"_value\"])\n  |> group()\n  |> rename(columns: {_value: \"Battery Drop (%)\"})",
      "refId": "A"
    }
  ],
  "title": "Total Battery Drop by Mode",
  "type": "barchart"
}
